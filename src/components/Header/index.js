import React from 'react';
import Nav from '../Nav';

function Header(props) {
    
    return (
        <section className='header-section'>
            <h1>pottfolio</h1>
            <Nav />
        </section>
    );
}

export default Header;